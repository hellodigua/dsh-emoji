# 素材说明

当前运行时只内置用户提供的一组 40 张正面鲸鱼表情：

```text
源图：ChatGPT Image 2026年8月12日 23_38_20.png
尺寸：1254 × 1254
SHA-256：3b87fa433ca1ab058a4dcbc020f7e6d8e6c174a1c3587d649a2020544b67e3be
输出：40 张；ds_01～ds_40
语义：开心、难过、疑惑、吃瓜、生气、无语、狗头、宕机、中性、大笑、哭泣、流汗、思考、OK、点头、睡觉、委屈、偷看、赞同、比心、害羞、星星眼、笑哭、感动、惊恐、捂脸、白眼、叹气、抓狂、调皮、偷笑、呵呵、酷、庆祝、加油、感谢、抱歉、抱抱、拜托、鼓掌
```

`scripts/slice-deepseek-sheet.py` 按固定的 `8×5` 单元格避开标题和编号文字，以颜色主体和封闭轮廓去除白色背景，同时保留蓝色主体、白色腹部、眼白、眼泪以及问号、省略号、怒气、爱心等独立装饰。

全部输出统一为 `128×128 RGBA PNG`，四角透明。文件 ID 与总览图中的 1～40 编号严格一致，不保留旧测试版的编号兼容。旧侧身蓝鲸系列已从运行时素材目录、catalog 和发布包移除。脚本只接受上述 SHA-256，避免同尺寸但内容不同的图片覆盖已验证素材。

旧的 `assets/emoji/bilibili/` 与 `src/catalog.generated.ts` 暂作为迁移参考保留，不进入当前运行时 catalog，也不在 `package.json#files` 的发布白名单中。

代码采用 MIT。代码许可证不声明也不推定总览图、鲸鱼形象或二创表情素材的所有权与再分发许可。将仓库转为 public、发布 npm 包或以其他方式公开分发素材前，应由素材提供者确认来源、权利人和允许的分发范围。

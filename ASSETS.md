# 素材说明

v0.1 的 35 张 AVIF 来自本机上游仓库 `/absolute/path/to/emoji-source`，同步时固定 revision：

```text
3693240a2db6ec017944e595a09e8ae900b5549c
```

同步范围仅为 `emoji.json` 中 `platform=bilibili` 的条目及 `output/bilibili/*.avif`。代码采用 MIT；该代码许可证不声明也不推定平台表情素材的所有权或再分发许可。

当前状态：仅限私有原型和技术验证。将仓库转为 public、发布 npm 包或以其他方式公开分发这些素材前，必须另行确认来源、权利人和允许的分发范围。

# 素材说明

当前运行时只内置用户提供的一组 40 张正面鲸鱼表情：

```text
尺寸：1254 × 1254
源图 SHA-256：3b87fa433ca1ab058a4dcbc020f7e6d8e6c174a1c3587d649a2020544b67e3be
输出：40 张；ds_01～ds_40
语义：开心、难过、疑惑、吃瓜、生气、无语、狗头、宕机、中性、大笑、哭泣、流汗、思考、OK、点头、睡觉、委屈、偷看、赞同、比心、害羞、星星眼、笑哭、感动、惊恐、捂脸、白眼、叹气、抓狂、调皮、偷笑、呵呵、酷、庆祝、加油、感谢、抱歉、抱抱、拜托、鼓掌
```

`scripts/slice-deepseek-sheet.py` 按固定的 `8×5` 单元格避开标题和编号文字，以颜色主体和封闭轮廓去除白色背景，同时保留蓝色主体、白色腹部、眼白、眼泪以及问号、省略号、怒气、爱心等独立装饰。

全部输出统一为 `128×128 RGBA PNG`，四角透明。文件 ID 与总览图中的 1～40 编号严格一致。脚本只接受上述 SHA-256，避免同尺寸但内容不同的图片覆盖已验证素材。

`assets/emoji/bilibili/` 与 `src/catalog.generated.ts` 只作为开发参考，不进入当前运行时 catalog，也不在 `package.json#files` 的发布白名单中。

## 素材授权状态

代码采用 MIT。代码许可证不声明也不推定总览图、鲸鱼形象或二创表情素材的所有权与再分发许可。

素材提供者另提供了一张 `140×140 RGBA PNG` 形象参考图（SHA-256：`a41e6652b8ef3527a7f4b00340613dc0d6f915f3208b31e73cbf6bad0ef4ca23`）。该图片不是当前包内置的 40 张输出图，也没有可用的作者、版权或来源元数据。

仓库维护者确认当前 40 张运行时素材可以作为 `dsh-emoji` 的内置内容随 npm 包公开分发；项目也已被 DSH 官方精选展示。该确认不把素材纳入 MIT，也不授予脱离本项目单独复制、改编或再分发素材的权利；相关素材权利仍归原权利人所有。
